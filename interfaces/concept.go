/* An interface defines a set of methods.

Any type that implements those methods automatically satisfies the interface (no implements keyword like Java).

This makes Go’s type system flexible and powerful.

👉 Think: “If it walks like a duck and quacks like a duck, it’s a duck.” */