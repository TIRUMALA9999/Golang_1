/* A struct is a collection of fields (like a class without methods in OOP).

Used to model real-world entities (like Car, Person, Student).

Can attach methods to structs → makes Go support OOP-style programming (without classes).

Go uses composition (embedding) instead of inheritance. */