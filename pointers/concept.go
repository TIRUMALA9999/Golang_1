/* A pointer stores the memory address of a variable.

Operator & → address-of (get pointer).

Operator * → dereference (get value from pointer).

Useful for:

Modifying data inside functions (pass by reference).

Working with large structs without copying. */