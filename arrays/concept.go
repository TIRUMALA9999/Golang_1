//An array is a fixed-size sequence of elements of the same type.

//Its length is part of its type → [5]int is a different type from [10]int.

//All elements are initialized to zero values (0, "", false, etc.).

//Rare in day-to-day Go (slices are preferred), but important for interviews.